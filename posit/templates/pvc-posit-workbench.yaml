{{- $claim := index .Values "rstudio-workbench" "sharedStorage" "name" -}}
{{- $storage := index .Values "rstudio-workbench" "sharedStorage" "requests" "storage" -}}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ $claim }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "rstudio-workbench.labels" . | nindent 4 }}
spec:
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: "{{ $storage }}"
